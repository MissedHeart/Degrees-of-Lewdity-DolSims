>
		<<if _config.activeIcon is "img/misc/icon/ATfield_locked.png">>
			<<set _config.progress = Math.ceil(Math.clamp(100*((_config.value - (_config.minValue || 0)) / (_config.maxValue || 100)), 0, 100))>>
		<</if>>
		<<if _important>>